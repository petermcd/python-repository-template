[project]
name = "my-super-project"
version = "0.1.0"
description = "This is my super project that does amazing things."
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.4.2",
    "pytest-cov>=7.0.0",
]
